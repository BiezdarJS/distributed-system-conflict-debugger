import { ClientBadgePipe } from './client-badge-pipe';

describe('ClientBadgePipe', () => {
  it('create an instance', () => {
    const pipe = new ClientBadgePipe();
    expect(pipe).toBeTruthy();
  });
});
